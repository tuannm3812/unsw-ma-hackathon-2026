# Data Directory

Place your raw competition data files (like `Kiva_Loans_Sample.pkl` and `Kiva Data Dictionary.xlsx`) in this folder or in the root of the repository.

By default, large files inside this directory (e.g. `.csv`, `.pkl`, `.xlsx`, `.parquet`) are ignored by `.gitignore` to avoid pushing heavy files to GitHub.
